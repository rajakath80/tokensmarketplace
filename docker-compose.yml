services:

  postgres:
      image: postgres
      volumes:
        - ./db_data:/var/lib/postgresql/data
      environment:
        POSTGRES_DB: tok
        POSTGRES_USER: tokuser
        POSTGRES_PASSWORD: tok123$
      ports:
        - "5433:5432"
  
volumes:
  db_data: